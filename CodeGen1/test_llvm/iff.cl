class Main {
    main() : Int {
        {   3;
            let x : Bool, x: Int, y : Bool <- true in if y then let x : Bool, x : Int <- 3 in x + 2 else let z : Int, y : Int <- 3 in z + 3  fi;
            
        }
    };
};
