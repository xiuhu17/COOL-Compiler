class Main inherits IO {

    another() : Object {
	let r : Main z : Object <- 0, i : Int <- 3 + 4 in
	     isvoid r;
        let r : Main z : Object <- 0, i : Int <- 3 + 4 in
	     isvoid r;
         let r : Main z : Object <- 0, i : Int <- 3 + 4 in
	     isvoid r;
    };

};
