class Main {
    main() : Int {
        {   3;
            let x : Bool, x: Int, y : Bool <- true in if y then let x : Bool, x : Int <- 3, x : Bool in x  else let z : Int, y : Int <- 3, z : Bool in not z  fi;
            5;
        }
    };
};
